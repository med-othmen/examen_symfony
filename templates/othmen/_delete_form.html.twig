<form method="post" action="{{ path('othmen_delete', {'id': othman.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ othman.id) }}">
    <button class="btn">Delete</button>
</form>
